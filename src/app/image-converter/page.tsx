'use client';
import { useEffect, useState } from 'react';
import LocalToolLayout from '@/components/LocalToolLayout';

type Format='image/png'|'image/jpeg'|'image/webp';
export default function ImageConverterPage(){const [file,setFile]=useState<File|null>(null);const [preview,setPreview]=useState('');const [format,setFormat]=useState<Format>('image/webp');const [quality,setQuality]=useState(.9);const [busy,setBusy]=useState(false);useEffect(()=>()=>{if(preview)URL.revokeObjectURL(preview)},[preview]);const choose=(f:File)=>{if(preview)URL.revokeObjectURL(preview);setFile(f);setPreview(URL.createObjectURL(f))};const convert=async()=>{if(!file)return;setBusy(true);try{const bitmap=await createImageBitmap(file);const canvas=document.createElement('canvas');canvas.width=bitmap.width;canvas.height=bitmap.height;const ctx=canvas.getContext('2d');if(!ctx)throw new Error('Canvas unavailable');ctx.drawImage(bitmap,0,0);bitmap.close();const blob=await new Promise<Blob|null>(r=>canvas.toBlob(r,format,format==='image/png'?undefined:quality));if(!blob)throw new Error('This browser could not encode the selected format.');const url=URL.createObjectURL(blob);const a=document.createElement('a');const ext=format==='image/png'?'png':format==='image/jpeg'?'jpg':'webp';a.href=url;a.download=`${file.name.replace(/\.[^.]+$/,'')}.${ext}`;a.click();URL.revokeObjectURL(url)}finally{setBusy(false)}};return <LocalToolLayout title='Image Format Converter' description='Convert browser-supported image files to PNG, JPEG or WebP without uploading them.'><section className='mx-auto max-w-3xl rounded-xl bg-white p-6 shadow'><input type='file' accept='image/*' onChange={e=>e.target.files?.[0]&&choose(e.target.files[0])} className='w-full rounded border p-3'/>{preview&&<img src={preview} alt='Selected preview' className='mx-auto mt-5 max-h-80 max-w-full rounded border object-contain'/>}<div className='mt-5 grid gap-4 sm:grid-cols-2'><label>Output format<select value={format} onChange={e=>setFormat(e.target.value as Format)} className='mt-1 w-full rounded border p-2'><option value='image/webp'>WebP</option><option value='image/jpeg'>JPEG</option><option value='image/png'>PNG</option></select></label><label className={format==='image/png'?'opacity-50':''}>Quality: {Math.round(quality*100)}%<input disabled={format==='image/png'} type='range' min='.1' max='1' step='.05' value={quality} onChange={e=>setQuality(+e.target.value)} className='w-full'/></label></div><button disabled={!file||busy} onClick={convert} className='mt-5 rounded bg-sky-600 px-5 py-2 text-white disabled:opacity-50'>{busy?'Converting...':'Convert & download'}</button></section></LocalToolLayout>}
